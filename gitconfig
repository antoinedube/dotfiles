[user]
    name = Antoine Dubé
    email = antoine.dube@gmail.com

[core]
    editor = nvim
    deltaBaseCacheLimit = 1G

[color]
    ui = auto
    branch = auto
    diff = auto
    status = auto

[push]
    default = simple

[merge]
    conflictStyle = diff3

[status]
    showUntrackedFiles = all

[transfer]
    fsckobjects = true

[alias]
    ll = log -n 25 --graph --oneline --decorate
    dt = difftool

[diff]
    tool = icdiff

[difftool]
    prompt = false

[difftool "icdiff"]
    cmd = /usr/bin/icdiff --line-numbers $LOCAL $REMOTE

[branch]
	autosetuprebase = always
