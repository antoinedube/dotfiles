[user]
    name = Antoine Dubé
    email = antoine.dube@dti.ulaval.ca

[core]
    editor = nvim
    deltaBaseCacheLimit = 512M

[color]
    ui = auto
    branch = auto
    diff = auto
    status = auto

[push]
    default = simple

[merge]
    conflictStyle = diff3
    ff = no

[status]
    showUntrackedFiles = all

[transfer]
    fsckobjects = true

[alias]
    ll = log -n 50 --graph --oneline --decorate
    dt = difftool

[diff]
    tool = icdiff

[difftool]
    prompt = false

[difftool "icdiff"]
    cmd = /usr/bin/icdiff --line-numbers $LOCAL $REMOTE

[pull]
    rebase = true
